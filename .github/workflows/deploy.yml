name: Deploy Static Website + app.py

on:
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      REMOTE_USER: ${{ secrets.REMOTE_USER }}
      REMOTE_HOST: ${{ secrets.REMOTE_HOST }}
      GUNICORN_SERVICE: ${{ secrets.GUNICORN_SERVICE }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare SSH + rsync repo to remote staging (~/.deploy_tmp)
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.ssh"
          printf '%s\n' "${{ secrets.SSH_PRIVATE_KEY }}" > "$HOME/.ssh/id_rsa"
          chmod 600 "$HOME/.ssh/id_rsa"

          # no strict host checking in CI
          RSYNC_SSH="ssh -i $HOME/.ssh/id_rsa -o StrictHostKeyChecking=no"

          # Debug key AFTER creation
          echo "Key lines:"
          wc -l "$HOME/.ssh/id_rsa"
          head -n 5 "$HOME/.ssh/id_rsa"

          rsync -az --delete \
            --exclude ".git" \
            --exclude ".github" \
            --exclude "keyfile" \
            --exclude "README.md" \
            --exclude "site.tar.gz" \
            --exclude "deploy.sh" \
            -e "$RSYNC_SSH" ./ "$REMOTE_USER@$REMOTE_HOST:~/.deploy_tmp/"

      - name: Promote staging → /var/www/marquetteranch + reload nginx
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -eo pipefail
            TMP="$HOME/.deploy_tmp"
            sudo mkdir -p /var/www/marquetteranch
            sudo rsync -a --delete "$TMP"/ /var/www/marquetteranch/
            
            sudo chown -R jason:www-data /var/www/marquetteranch
            sudo chmod -R 775 /var/www/marquetteranch
            
            sudo systemctl reload nginx || true
            
            echo "Live site contents:"
            sudo ls -la /var/www/marquetteranch | head -n 40

      - name: Restart Gunicorn
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            sudo systemctl restart ${{ secrets.GUNICORN_SERVICE }} || true
